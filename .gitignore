# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node modules (Dependencies)
node_modules
.DS_Store

# Distribution / Build output
# We ignore this because 'npm run build' generates it fresh every time.
# The 'gh-pages' script handles pushing this to the correct branch automatically.
dist
dist-ssr
coverage
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# Environment Variables
# (If you ever decide to move your API keys to a .env file)
.env
.env.*
!.env.example

# Vite specific
.vite/